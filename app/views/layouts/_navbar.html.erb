<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">
  <a class="navbar-brand" href="/">Tabitta</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <%= link_to "Anonymous", anonymouses_path, class: "text-white" %>
    </ul>
    <ul class="navbar-nav mr-auto">
        <%= link_to "Search", search_index_path, class: "text-white" %>
    </ul>
    <ul class="navbar-nav mr-3 navbar-right">
      <%= link_to current_user&.name, user_path(current_user&.id), class: "text-white"  if current_user.present? %>
    </ul>

    <ul class="navbar-nav navbar-right">
      
      <% if user_signed_in? %>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-white" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "text-white" %>
      <% end %>

    </ul>
  </div>
</nav>